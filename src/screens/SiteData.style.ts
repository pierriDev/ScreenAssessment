import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    container: {
        flex: 1
    },
    emptyArea: {
        paddingVertical: 134,
        backgroundColor: "#FFF"
    }
});